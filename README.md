
  # Design all pages with transitions

  This is a code bundle for Design all pages with transitions. The original project is available at https://www.figma.com/design/Pj81gEltWVSRqzA2hxFPh3/Design-all-pages-with-transitions.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  